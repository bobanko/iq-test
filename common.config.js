export const colors = {
  red: "var(--red)",
  green: "var(--green)",
  blue: "var(--blue)",
  yellow: "var(--yellow)",
  black: "var(--black)",
  white: "var(--white)",
};

export const rgbColors = [colors.red, colors.green, colors.blue];

export const defaultColors = [
  colors.red,
  colors.green,
  colors.blue,
  colors.yellow,
];
